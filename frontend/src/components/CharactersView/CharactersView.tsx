import { FC } from "react";
import { CharactersViewProps } from "../../common/types";

const CharactersView: FC<CharactersViewProps> = (props) => {
  return <div>CharactersView</div>;
};

export default CharactersView;
